version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: gunicorn server.wsgi:application --bind 0.0.0.0:8001
    ports:
      - 8001:8001
    volumes:
    - ./:/usr/src/app